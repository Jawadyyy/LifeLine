<!-- Status-bar notification icon. Android masks this to a white silhouette on
     the notification accent colour, so only the shape (alpha) matters — use a
     transparent background with a clean white medical cross, NOT a filled
     shape (a filled shape renders as a white square/blob). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M14,3h-4v7H3v4h7v7h4v-7h7v-4h-7z" />
</vector>
