LIFELINE - PLAY STORE LAUNCH TODO
(updated 2026-07-10 - post third audit)

=== BLOCKERS ===
(none - all resolved)

1. [DONE] Privacy Policy - live at https://lifeline-relay.vercel.app/privacy.html
   (hosted on the Vercel relay, relay/public/privacy.html).
   -> Remaining manual step: paste URL into Play Console -> App content -> Privacy policy.
2. [DONE] iOS bundle ID fixed to com.lifeline.app.
3. [DONE] debugPrint calls replaced with logDebug() (kDebugMode-gated).
4. [DONE] Calls implemented end-to-end: Agora RTC + Firestore signaling +
   FCM ring-when-closed via relay + token endpoint (/api/agora-token).
   Callee ring timeout + busy guard added.
5. [DONE] Chat complete: emoji, images, voice notes, call log entries,
   edit/delete/copy, presence, typing indicator. All media on Supabase.
   ImgBB + firebase_storage removed.
6. [DONE] Push relay deployed (https://lifeline-relay.vercel.app), PUSH_RELAY_URL set.
7. [DONE] Chat UI fully localized (EN/UR parity, ~30 new keys 2026-07-10).
8. [DECIDED 2026-07-10] GEMINI_KEY + GEOAPIFY_KEY ship in APK (.env is a bundled
   asset). Accepted for v1: both are free-tier keys with no billing attached -
   worst case is quota burn, no cost. Gemini key restricted to Generative
   Language API; Geoapify capped at 3k req/day by plan.

=== LAUNCH CHECKLIST (manual, before submission) ===
- Build AAB for upload: flutter build appbundle --release (NOT the apk).
- Ensure key.properties + upload keystore present at build time (release build
  silently falls back to DEBUG signing without it - Play rejects).
- Play Console: privacy policy URL (see #1), data safety form (declare:
  location, contacts, health info, messages, audio; all encrypted in transit;
  deletion via email), content rating, target audience 13+.
- Smoke test on 2 physical devices: SOS flow, call (app open + app killed),
  voice note, image, profile pic change, Urdu locale.

=== V1.1 BACKLOG ===
- Proxy Gemini + Geoapify through the Vercel relay so keys leave the APK
  (same pattern as /api/send; decided to defer 2026-07-10).
- Firebase SDK major upgrades (core 3->4, auth 5->6, firestore 5->6, etc).
  Works fine on current majors; upgrade + test post-launch.
- Terms of Service page (privacy policy covers the hard requirement).
- Call feature tests (zero coverage; service+screen).
- Reciprocal contacts: when A adds B, B should see A automatically
  (add lightweight entry with addedByPeer flag; discussed 2026-07-09).
- Fix known-flaky ChatEmptyState/medical_id widget tests (missing l10n
  delegate in test harness; pre-existing).
- Add About screen with version + privacy policy link in-app.
- Navbar accent #FF6F61 doesn't match brand #EF5A2A (one-line fix).
- Verify WRITE_CONTACTS permission still needed.
- Phone number OTP verification (deferred for cost; Truecaller SDK is the
  $0 option if ever needed).

=== NOT AN ISSUE (checked) ===
- Firebase client keys in firebase_options.dart: public by design.
- SUPABASE_ANON_KEY / AGORA_APP_ID in APK: public by design.
- No secrets committed to git (.env untracked, relay secrets in Vercel env only).
